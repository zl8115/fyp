import numpy as np

class algo(object):
	def __init__(self):
		self.order = 5
		self.lens = np.zeros((self.order,1))
		self.out = np.zeros((self.order,1))
		self.rel = 0
		self.mu = 0
		self.sig = 0
		self.amp = 0

	def predEst(self):
		mu


